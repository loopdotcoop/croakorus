Router.map(function() {
    this.route('track.make', {
            path: '/track/make'
        }
    );
    this.route('track.play', {
            path: '/track/play'
        }
    );
    this.route('tracks.list', {
            path: '/tracks/list'
        }
    );
});
